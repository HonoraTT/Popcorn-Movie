<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.movietime.dao.SeatDao">
    <update id="update">
        UPDATE seat
        <set>
            <if test="isBooked != null">
                <choose>
                    <when test="isBooked">status = 'booked'</when>
                    <otherwise>status = 'available'</otherwise>
                </choose>
            </if>
        </set>
        WHERE show_id = #{showId} AND `row_number` = #{row} AND `column_number` = #{col}
    </update>

    <select id="occupied" resultType="java.lang.Boolean">
        SELECT CASE WHEN status = 'booked' THEN true ELSE false END as is_booked 
        FROM seat
        WHERE show_id = #{showId} AND `row_number` = #{row} AND `column_number` = #{col} FOR UPDATE
    </select>

    <select id="findAllBooked" resultType="Seat">
        SELECT 
            id,
            show_id as showId,
            `row_number` as rowNum,
            `column_number` as colNum,
            CASE WHEN status = 'booked' THEN true ELSE false END as isBooked
        FROM seat
        WHERE show_id = #{id} AND status = 'booked'
    </select>
</mapper>